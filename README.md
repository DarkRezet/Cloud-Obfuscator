# 🔒 Cloud Obfuscator

![Python](https://img.shields.io/badge/Python-3.8+-blue?style=for-the-badge&logo=python)\
![AES-GCM](https://img.shields.io/badge/Encryption-AES--GCM-purple?style=for-the-badge&logo=keepassdx)\
![C
Extension](https://img.shields.io/badge/C%20Extension-keymodule-green?style=for-the-badge&logo=c)\
![Security](https://img.shields.io/badge/Security-High-critical?style=for-the-badge&logo=shield)\
![Status](https://img.shields.io/badge/Status-Stable-brightgreen?style=for-the-badge&logo=checkmarx)

**Cloud Obfuscator** --- инструмент, который я сделал для ситуаций,
когда обычной обфускации мало,\
а защищать код всё-таки нужно. Проект делает ставку на цепочку:\
обфускация AST -> компрессия -> шифрование AES‑GCM -> C‑модуль -> привязка к
HWID.

В результате получается самоисполняемый `.py`, который работает как
обычный скрипт,\
но разобрать его значительно сложнее.

------------------------------------------------------------------------

## ✨ Что умеет

### 🔹 Мутация кода

Лёгкая обфускация через AST: добавляются шумовые инструкции,
перестановки и прочие мелочи.

### 🔹 Шифрование и упаковка

-   Сжатие: LZMA → zlib\
-   Разбиение на случайные чанки\
-   Шифрование каждого чанка AES‑GCM со своим nonce

### 🔹 Привязка к железу

Ключ собирается из: - `secret.bin`\
- HWID машины\
- Salt, встроенного в loader

### 🔹 Self‑contained loader

Готовый `.cloud.obf.py` содержит: - только зашифрованные бинарные
данные\
- salt\
- код расшифровщика\
- запуск в памяти, без записи исходника на диск

------------------------------------------------------------------------

## 📦 Структура проекта

    .
    ├── cloud_obfuscator.py   # основной инструмент
    ├── keymodule.c           # C‑расширение с логикой ключей
    ├── setup.py              # сборка C‑модуля
    └── your_script.py        # ваш исходный скрипт

------------------------------------------------------------------------

## 🔧 Установка

Установить зависимости:

``` bash
pip install pycryptodome
```

Собрать C‑модуль:

``` bash
python setup.py build_ext --inplace
```

------------------------------------------------------------------------

## 🚀 Использование

Обфусцировать Python‑файл:

``` bash
python cloud_obfuscator.py your_script.py
```

После запуска появится файл:

    your_script.cloud.obf.py

Запуск:

``` bash
python your_script.cloud.obf.py
```

------------------------------------------------------------------------

## 🧠 Как работает

1.  Код читается и слегка мутируется.\
2.  Сжимается LZMA → zlib.\
3.  Разбивается на чанки случайного размера.\
4.  Каждый chunk шифруется AES‑GCM своим nonce.\
5.  В loader вшивается salt.\
6.  При запуске loader вычисляет ключ через
    `keymodule.derive_key(salt)`,\
    расшифровывает весь буфер и исполняет его в памяти.

------------------------------------------------------------------------

## 🛡 Безопасность

-   AES‑GCM обеспечивает защиту и контроль целостности\
-   Привязка к HWID не позволяет запустить файл на другой машине\
-   C‑модуль затрудняет реверс логики генерации ключа\
-   Сжатие и AST‑обфускация ломают статический анализ

Это не абсолютная защита, но уровень заметно выше стандартных
обфускаторов.

------------------------------------------------------------------------

## ⚠ Важно

-   `secret.bin` должен находиться рядом с loader‑файлом\
-   Без секрета и оригинального HWID код не запустится\
-   Инструмент подходит не для коммерческой защиты, а для усложнения
    анализа

------------------------------------------------------------------------

# 👤 Автор

## **ROZETKA / DARKREZET**

![Author](https://img.shields.io/badge/Author-ROZETKA%20%2F%20DARKREZET-black?style=for-the-badge&logo=github)
